import 'package:flutter/material.dart';

abstract final class AppColors {
  // Background
  static const background = Color(0xFF0D0F10);

  // Surfaces
  static const surface = Color(0xFF151819);
  static const surfaceElevated = Color(0xFF1C2022);

  // Brand
  static const primary = Color(0xFFC88A2B);
  static const primaryLight = Color(0xFFE0A84B);

  // Text
  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFF9A9FA3);

  // Borders
  static const border = Color(0xFF2A2E30);

  // Semantic
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFB74D);
  static const error = Color(0xFFE57373);
}